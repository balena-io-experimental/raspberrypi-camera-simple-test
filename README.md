# raspberrypi-camera-simple-test

Set the following  configuration variables:

* `BALENA_HOST_CONFIG_startx` = `1`
* `BALENA_HOST_CONFIG_gpu_mem` = `144` ( *this value may need to change based on what rpi you are using* )
